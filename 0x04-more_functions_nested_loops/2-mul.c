#include "main.h"

/**
 * mul - description to multiply two integers int a and int b
 * @a: input integer
 * @b: input integer
*
 * Return : always return 0 for mul
 */
int mul(int a, int b)

{
	return (a * b);
}
